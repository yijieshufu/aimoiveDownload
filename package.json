{
  "name": "aimovie-download",
  "private": true,
  "description": "一键同时启动后端热重载 + 前端 Vite HMR（开发用）",
  "scripts": {
    "dev:prepare": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/dev-prepare.ps1",
    "dev:backend": "python -m uvicorn backend.main:app --host 127.0.0.1 --port 8003 --reload",
    "dev:frontend": "npm run dev --prefix frontend -- --host 127.0.0.1 --port 5173",
    "dev": "npm run dev:prepare && concurrently -n backend,frontend -c cyan,magenta \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
